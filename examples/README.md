# Examples

| File | What it does |
| --- | --- |
| `quickstart.py` | Loads the bundled demo database and runs a self-query search. Smallest end-to-end demo. |

Run any example from the repository root after installing the package:

```bash
pip install -e .
python examples/quickstart.py
```
